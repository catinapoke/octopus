using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(ForTestingSomeThings))]
public class CustomInspector : Editor {
    
    public override void OnInspectorGUI()
    {
        DrawDefaultInspector();

        ForTestingSomeThings myScript = (ForTestingSomeThings)target;
        if (GUILayout.Button("Test clone"))
        {
            myScript.InstantiateTest();
        }
        if (GUILayout.Button("InActiveBase"))
        {
            myScript.InActiveBase();
        }
        if (GUILayout.Button("InActiveTest"))
        {
            myScript.InActiveTest();
        }
        if (GUILayout.Button("Change base"))
        {
            myScript.Changebase();
        }
    }
}
*/